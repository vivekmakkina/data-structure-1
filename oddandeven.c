#include <stdio.h>
int main() {
    int num;
    printf("Enter an integer: ");
    scanf("%d", &num);
    if (num % 2 == 0)
    printf("the entered number is even");
    else
    print("the given number is odd");
    return 0;
}


